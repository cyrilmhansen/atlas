# E-M2 owner trial 1

Date: 2026-07-15

Status: initial response completed against `c31fbe7`; corrected confirmation
passed against `5563c27`

Protocol: `docs/phase3/e-m2-protocol.md`

## Participant boundary

The participant is the project owner and is technically literate, but already
knows Atlas. This is real human feedback and can justify corrections. It is not
independent or blind evidence and cannot alone close the representative visitor
gate.

## Response

The response identified `order.is_sorted.adjacent`, reported worst time `O(n)`
as declarative information and found the local observation panel. It described
execution as the Rust implementation and treated the remaining workbench
information broadly as repository declarations.

For BFS, the response associated `Theta(V + E)` with the subject and asked
whether Rust build tests determined what to do.

## Adjudication

| Dimension | Result | Observation |
|---|---|---|
| Exact adjacent identity | Pass | Exact algorithm ID retained |
| Adjacent time value | Pass | `O(n)` found |
| Adjacent evidence | Partial | Declarative nature seen, exact source omitted |
| Interactive backend | Fail | Private WASM visual model confused with registered Rust implementation |
| Local observation | Partial | Observation panel found; other evidence over-generalized as declared |
| Exact return path | Not observed | No result supplied |
| Exact BFS identity | Partial | Traversal and shortest-path complexity boundaries appear conflated |
| BFS implementation evidence | Fail | Behavioral Rust test and inferred complexity remained ambiguous |
| Missing execution restraint | Not observed | No conclusion supplied |

## Corrections justified

1. Name the interactive path as a WASM model and state that registered
   implementation evidence is separate.
2. Preserve complete claim level and provenance in the workbench instead of
   truncating the source.
3. Label implementation tests as test evidence without promoting them to an
   algorithmic complexity claim.

No response justifies another executable algorithm, backend, DatasetSpec,
schema field or evidence level.

## Corrected confirmation

After the targeted corrections, the owner confirmed:

1. the `Open WASM model` backend boundary is unambiguous;
2. `declared` and `inferred` evidence pills on breadth-first traversal are
   visually distinct and clearly mean different things;
3. `Registry evidence` returns clearly to the correct fiche.

The three corrected dimensions therefore pass the informed-owner confirmation.
The result supports publishing the E-M2 candidate, but does not remove the
participant-boundary limitation: an independent visitor has not yet attempted
the protocol.
